exclude: |
  (?x)
  # NOT INSTALLABLE ADDONS
  # END NOT INSTALLABLE ADDONS
  # Files and folders generated by bots, to avoid loops
  ^setup/|/static/description/index\.html$|
  # We don't want to mess with tool-generated files
  .svg$|/tests/([^/]+/)?cassettes/|^.github/|^eslint.config.cjs|^prettier.config.cjs|
  # Maybe reactivate this when all README files include prettier ignore tags?
  ^README\.md$|
  # Library files can have extraneous formatting (even minimized)
  /static/(src/)?lib/|
  # Repos using Sphinx to generate docs don't need prettying
  ^docs/_templates/.*\.html$|
  # Don't bother non-technical authors with formatting issues in docs
  readme/.*\.(rst|md)$|
  # Ignore build and dist directories in addons
  /build/|/dist/|
  # Ignore test files in addons
  /tests/samples/.*|
  # You don't usually want a bot to modify your legal texts
  (LICENSE.*|COPYING.*)|
  # Exclude full docs tree and archived modules from all hooks
  ^docs/|^archived_modules/
default_language_version:
  python: python3
  node: "22.9.0"
repos:
  - repo: local
    hooks:
      # These files are most likely copier diff rejection junks; if found,
      # review them manually, fix the problem (if needed) and remove them
      - id: forbidden-files
        name: forbidden files
        entry: found forbidden files; remove them
        language: fail
        files: "\\.rej$"
      - id: en-po-files
        name: en.po files cannot exist
        entry: found a en.po file
        language: fail
        files: '[a-zA-Z0-9_]*/i18n/en\.po$'
  - repo: https://github.com/sbidoul/whool
    rev: v1.3
    hooks:
      - id: whool-init
        additional_dependencies: [setuptools]
  - repo: https://github.com/OCA/odoo-pre-commit-hooks
    rev: v0.1.7
    hooks:
      - id: oca-checks-odoo-module
        additional_dependencies: [setuptools]
      - id: oca-checks-po
        additional_dependencies: [setuptools]
        args:
          - --disable=po-pretty-format
  - repo: local
    hooks:
      - id: prettier
        name: prettier (with plugin-xml)
        entry: prettier
        args:
          - --write
          - --list-different
          - --ignore-unknown
        types: [text]
        files: \.(css|htm|html|js|json|jsx|less|md|scss|toml|ts|xml|yaml|yml)$
        language: node
        additional_dependencies:
          - "prettier@3.3.3"
          - "@prettier/plugin-xml@3.4.1"
  - repo: local
    hooks:
      - id: eslint
        name: eslint
        entry: eslint
        args:
          - --color
          - --fix
        verbose: true
        types: [javascript]
        language: node
        additional_dependencies:
          - "eslint@9.12.0"
          - "eslint-plugin-jsdoc@50.3.1"
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
        # exclude autogenerated files
        exclude: /README\.rst$|\.pot?$
      - id: end-of-file-fixer
        # exclude autogenerated files
        exclude: /README\.rst$|\.pot?$
      - id: debug-statements
      - id: fix-encoding-pragma
        args: ["--remove"]
      - id: check-case-conflict
      - id: check-docstring-first
      - id: check-executables-have-shebangs
      - id: check-merge-conflict
        # exclude files where underlines are not distinguishable from merge conflicts
        exclude: /README\.rst$|^docs/.*\.rst$
      - id: check-symlinks
      - id: check-xml
      - id: mixed-line-ending
        args: ["--fix=lf"]
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.8
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format
  - repo: https://github.com/OCA/pylint-odoo
    rev: v9.1.3
    hooks:
      - id: pylint_odoo
        name: pylint with optional checks
        args:
          - --rcfile=.pylintrc
          - --exit-zero
        additional_dependencies: [setuptools]
        verbose: true
      - id: pylint_odoo
        args:
          - --rcfile=.pylintrc-mandatory
        additional_dependencies: [setuptools]
  # ============================================================================
  # Odoo Custom Linting Rules
  # Based on docs/principles/ - see scripts/lint/README.md for details
  # ============================================================================
  - repo: local
    hooks:
      # Phase 1: Simple pattern-based checks (pygrep)
      - id: odoo-no-assertraises-tuple
        name: "Odoo: No tuple in assertRaises"
        description: "Odoo's assertRaises doesn't support tuple of exceptions like stdlib unittest"
        entry: 'self\.assertRaises\s*\(\s*\('
        language: pygrep
        types: [python]
        files: /tests/
      - id: odoo-no-pii-in-logs
        name: "Odoo: No PII in log messages"
        description: "Detect potential PII (name, phone, email, etc.) in log statements"
        entry: '_logger\.(debug|info|warning|error|critical)\([^)]*\.(name|national_id|phone|mobile|email|address|birth_date|tax_id|bank_account)\b'
        language: pygrep
        types: [python]
        exclude: ^scripts/|/tests/
      # Phase 2: Naming conventions (mandatory)
      - id: odoo-check-naming
        name: "Odoo: Naming conventions"
        description: "Check model and field naming conventions"
        entry: python scripts/lint/check_naming.py
        language: python
        additional_dependencies:
          - PyYAML
        types: [python]
        # Exclude: scripts, tests, migrations, and third-party modules
        exclude: ^scripts/|/tests/|migrations/|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      - id: odoo-check-xml-ids
        name: "Odoo: XML ID naming conventions"
        description: "Check XML ID naming patterns for views, actions, menus, groups"
        entry: python scripts/lint/check_xml_ids.py
        language: python
        additional_dependencies:
          - PyYAML
        types: [xml]
        # Exclude: scripts, tests, data, demo, and third-party modules
        exclude: ^scripts/|/tests/|/data/|/demo/|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      # Phase 2: ACL check (warning only, runs on all files)
      - id: odoo-check-acl
        name: "Odoo: ACL files exist"
        description: "Check that modules have ir.model.access.csv"
        entry: python scripts/lint/check_acl.py --check-only
        language: python
        additional_dependencies:
          - PyYAML
        pass_filenames: false
        always_run: true
      # Phase 2: Compliance check (security spec validation)
      - id: odoo-compliance-check
        name: "Odoo: Security compliance check"
        description: "Validate modules with compliance.yaml against actual security config"
        entry: python -m scripts.compliance.checker --all
        language: python
        additional_dependencies:
          - PyYAML
        pass_filenames: false
        always_run: true
      # Phase 3: Performance checks (warning only)
      - id: odoo-check-performance
        name: "Odoo: Performance anti-patterns"
        description: "Check for offset pagination, cr.commit() in loops, N+1 queries"
        entry: python scripts/lint/check_performance.py --check-only
        language: python
        additional_dependencies:
          - PyYAML
        types: [python]
        # Exclude: scripts, tests, migrations, and third-party modules
        exclude: ^scripts/|/tests/|migrations/|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      # Phase 3: Logger setup check (warning only)
      - id: odoo-check-logger
        name: "Odoo: Logger setup and PII"
        description: "Check logger setup pattern and potential PII in logs"
        entry: python scripts/lint/check_logger.py
        language: python
        additional_dependencies:
          - PyYAML
        types: [python]
        # Exclude: scripts, tests, manifests, inits, and third-party modules
        exclude: ^scripts/|/tests/|__manifest__|__init__|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      # Phase 3: UI patterns check (warning only)
      - id: odoo-check-ui
        name: "Odoo: UI patterns"
        description: "Check list limits, sample data, XPath syntax, statusbar location, extension points"
        entry: python scripts/lint/check_ui_patterns.py
        language: python
        additional_dependencies:
          - PyYAML
        types: [xml]
        # Exclude: scripts, tests, data, demo, and third-party modules
        exclude: ^scripts/|/tests/|/data/|/demo/|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      # Odoo 19 Compatibility Checks
      - id: odoo-check-odoo19-python
        name: "Odoo: Odoo 19 compatibility (Python)"
        description: "Check Command API tuples, group_expand signature"
        entry: python scripts/lint/check_odoo19.py
        language: python
        additional_dependencies:
          - PyYAML
        types: [python]
        # Exclude: scripts, tests, migrations, and third-party modules
        exclude: ^scripts/|/tests/|migrations/|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      - id: odoo-check-odoo19-xml
        name: "Odoo: Odoo 19 compatibility (XML)"
        description: "Check search view group attrs, XPath @title"
        entry: python scripts/lint/check_odoo19.py --xml
        language: python
        additional_dependencies:
          - PyYAML
          - lxml
        types: [xml]
        # Exclude: scripts, tests, data, demo, and third-party modules
        exclude: ^scripts/|/tests/|/data/|/demo/|^(fastapi|queue_job|base_user_role|extendable|extendable_fastapi|endpoint_route_handler)/
        pass_filenames: true
      # API authentication enforcement
      - id: odoo-check-api-auth
        name: "Odoo: API endpoint authentication"
        description: "Verify all API endpoints require authentication (allowlist for public)"
        entry: python scripts/audit-api-auth.py --strict
        language: python
        pass_filenames: false
        always_run: false
        files: .*/routers/.*\.py$
  - repo: https://github.com/zricethezav/gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.10
    hooks:
      - id: bandit
        args: ["-c", "pyproject.toml", "-r", "-ll"]
        additional_dependencies: [setuptools]
        exclude: ^(tests/|scripts/tests/|.*_test\.py$|test_.*\.py$)
  - repo: https://github.com/semgrep/semgrep
    rev: v1.153.0
    hooks:
      - id: semgrep
        args: ["--config", ".semgrep/", "--error", "--quiet"]
        additional_dependencies: [setuptools]
        # Exclude test files, migrations, and non-module directories
        exclude: ^(tests/|scripts/|docs/|tools/|.*/tests/.*|.*/migrations/.*)
